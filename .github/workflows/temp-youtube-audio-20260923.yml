name: Temporary Loader progress check

on:
  push:
    paths:
      - ".github/workflows/temp-youtube-audio-20260923.yml"

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check completed conversion
        shell: bash
        run: |
          curl -LfsS "https://lto2.affadaffa.com/api/progress?id=qIM7f1yFJ49k9p5MPn7Vey3" | jq .
